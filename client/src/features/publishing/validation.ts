export const addressPattern = '[A-Za-z0-9](?:[A-Za-z0-9.-]*[A-Za-z0-9])?';
export const maxAddressLength = 80;
export const initialHtml = `<style>body{font-family:Georgia,serif;max-width:40rem;margin:3rem auto;color:#293430;line-height:1.7}h1{font-weight:normal}a{color:#9d543d}</style><h1>A new corner of the small web</h1><p>Write something that deserves an address. A note, a list, a recipe, a map.</p>`;
